# 蕾缪安：接入与维护

知识版本 0.3.2、人格 0.3.1、表达参考 0.3.5。已完成接入实现与原生 AstrBot 隔离联调；2026-09-24 在 njuse 启用私聊，现覆盖全部 QQ 私聊与群聊。

## 文件放在哪里

| 目录 | 内容 |
| --- | --- |
| `knowledge/lemuen/` | 唯一的资料源、人格、表达参考、出处及校对说明 |
| `plugins/astrbot_plugin_lemuen/` | 小插件：调用原生检索、选择表达参考、标记说话者 |
| `scripts/lemuen.py` | `check`、`build`、`smoke` 三个维护命令 |
| `tests/fixtures/lemuen.json` | 少量事实与聊天回归用例 |
| `runtime/lemuen/build/` | 可重新生成的导入文件、人格副本、插件 ZIP 和私聊分段设置 |
| `runtime/lemuen/latest-smoke.*` | 最近一次实际联调结果，后续运行覆盖 |
| `runtime/research/lemuen/` | 原文缓存，用于复核出处，不进入 Git |

多轮评测报告、旧模型比较脚本、重复主题 Markdown 和生成文档不再随仓库保留。出处与人物资料没有删减。

## 运行方式

人格使用 AstrBot 原生人格管理，69 个原作块和 1 个 EP 歌词块使用原生知识库管理。插件只在“已启用 + 会话匹配允许规则 + 当前选择蕾缪安人格”同时成立时运行。规则支持通配符，`napcat:FriendMessage:*` 覆盖当前及未来所有 QQ 私聊；`napcat:GroupMessage:*` 覆盖全部群聊。

每轮系统提示词固定加入 `knowledge/lemuen/voice-lines.json` 的 38 条游戏语音全文，正文共 1,522 字，保留场景标题。语音随蕾缪安插件 0.3.0 打包，用于已允许的私聊、群聊和主动私聊；即使检索未命中或超时也保留。来源为已核对的游戏台词资源 V，构建时可用原文缓存逐条校验。战斗、生日等原场景保留各自语境，信赖标题不引入养成过程。

每轮另从原生知识库取最多五条资料，再按既有规则选择表达参考。人际经历参考仍采用“话题词 + 召回条目”的简单规则；日常兴趣与对见闻的好奇作为稳定背景，具体经历仍按检索结果使用。检索超时或失败时保留人格与常驻表达，跳过依赖检索的经历参考。

说话者标记跟随用户消息保存，检索资料和表达参考仅用于当前请求。群聊另由 Rolebot 提供最近 6 条或 3 分钟内消息的并集作为当轮背景，包含未触发回复的群消息，详见 [群聊策略](plugins.md#群聊策略与管理)。群聊检索最多参考当前说话者上一次消息，避免另一位成员的话题直接接到他身上；模型仍能看到 AstrBot 提供的会话上下文。后续已添加按好友隔离的长期记忆，见 [插件配置](plugins.md)；不设养成机制。

推荐不在该会话的原生自动知识库列表重复选择“蕾缪安”，由插件调用原生检索接口；如果 AstrBot 已注入同一知识库，插件复用已有结果。此时检索问句按 AstrBot 原生规则处理。时间线与知情字段随完整块提供，仍不是结构化的检索过滤条件。

## EP 歌词

《Sanctuary Inside》按用户提供的 52 行英文全文收录在 `entries.json` 的 L108，来源 UEP1。包括重复副歌，整首作为一个检索块，按歌名、EP 或歌词片段召回。它属于歌曲的抒情表达，不据其中意象编造角色经历或与用户的关系。未做外部原文或音频校对。

前端进入「知识库」→「蕾缪安」→「蕾缪安-角色EP与歌曲.md」可查看完整内容。向已有实例添加时，仅导入该新文档，避免重新导入其他文档造成重复；常驻系统提示词仍由 38 条角色语音组成。

## 检查与打包

```bash
conda activate astrbot-wsl
python scripts/lemuen.py check
python scripts/lemuen.py build
python -m unittest discover -s tests -v
```

这些命令不调用收费 API。构建产物在 `runtime/lemuen/build/`。普通 CI 同样只做离线检查、测试与打包。

原生联调显式调用 API：

```bash
python scripts/lemuen.py smoke --live
# 只检查部分场景
python scripts/lemuen.py smoke --live --cases D07,G01
```

默认通过 SSH `njuse` 创建临时 AstrBot 根目录。嵌入配置取自本地 `.env`，聊天提供商取自服务器当前启用配置；密钥不写入结果或仓库。临时实例不加载 QQ 适配器，也不启动 WebUI、systemd 服务或 NapCat。结束后删除临时数据库，保留本地最近一次合成对话结果。真实会话记录不会被读取。

## njuse 当前配置

2026-09-24 已确认机器人 QQ 登录成功（`ready`），NapCat 与 AstrBot 的 OneBot WebSocket 连接已建立；AstrBot 服务运行、NapCat 容器健康。全部好友可直接私聊，未来新增好友也在范围内。本次没有代发真实 QQ 消息，实际回复可由用户在 QQ 中验证。

- 原生配置：**蕾缪安 · QQ私聊**，匹配 `napcat:FriendMessage:*`，包含未来的新好友。
- 私聊默认人格“蕾缪安”，聊天模型 `deepseek-v4-pro`，插件 0.3.0 已启用。
- 原生知识库“蕾缪安”：10 份文档、70 个完整资料块（含 1 个 EP 歌词块）；嵌入为 `qwen3.7-text-embedding`、1024 维。
- 群聊使用独立配置「蕾缪安 · QQ群聊」，匹配 `napcat:GroupMessage:*`；同一人格和模型，各群独立上下文。回复、复读和管理规则见 [群聊策略](plugins.md#群聊策略与管理)。
- 前端：<http://127.0.0.1:16185>。修改私聊设置时，在配置页面选择“蕾缪安 · QQ私聊”。

后续新服务器或重新部署时，可按下面步骤配置：

1. 在本地运行 `python scripts/lemuen.py build`。
2. 经 SSH 隧道打开 AstrBot WebUI：<http://127.0.0.1:16185>。
3. 添加 OpenAI 兼容嵌入提供商：Base URL `https://dashscope.aliyuncs.com/compatible-mode/v1`，模型 `qwen3.7-text-embedding`，维度 1024，维度发送模式 `always`。Key 从自己的 `.env` 配置到后台，不写入人格文件。
4. 创建名为“蕾缪安”的知识库，选择该嵌入提供商。稠密和稀疏候选各 20，最终返回 5，重排序暂不配置。通过预分块 JSON 导入生成的 `astrbot-import.json`；每条资料作为完整块。
5. 创建名为“蕾缪安”的人格，填入生成的 `persona.md`。初期不配置开场共同往事、工具和技能。
6. 从本地 ZIP 安装 `astrbot_plugin_lemuen.zip`。安装默认关闭；在插件配置中填写允许的会话规则，再启用。全部 QQ 私聊使用 `napcat:FriendMessage:*`；空列表不作用于任何会话。
7. 为 QQ 私聊创建独立 AstrBot 配置，默认人格设为“蕾缪安”，并绑定同一规则 `napcat:FriendMessage:*`。群聊随后按 [插件说明](plugins.md#群聊策略与管理) 建立独立配置。初次接入时可为现有私聊新建会话，原会话保留；日常更新人格不需要清空聊天。
8. 将生成的 `private-chat-settings.json` 中设置合并到此私聊配置：启用原生分段回复、仅对模型回复生效、关闭流式回复。它是少量设置项，不能整体替换包含模型等设置的原配置。

停用时先移除私聊/群聊到对应蕾缪安配置的绑定，再关闭插件，并将相关会话换回默认人格。单独关闭插件只停止检索与表达补充，不会撤销原生人格选择。

初次启用备份位于服务器 `runtime/backups/lemuen-private-20260924T074535Z/`；后续更新的最新备份路径记录在 `runtime/lemuen/activation.json`，其中同时保存当前版本和启用范围。备份包含私密配置与数据库，勿提交 Git。

人物资料更新后重新检查、打包、更新对应知识库；修改表达参考需要更新插件 ZIP，修改人格需要更新原生人格。插件不在启动时自动覆盖后台内容。更换嵌入模型或维度需重建索引。

## 连续消息

普通闲聊由模型组织为 2–4 个短段落，AstrBot 按空行把它们发送成独立 QQ 消息，随机间隔 0.8–1.8 秒；同一段内的句号、问号保留。段数是表达偏好，短确认与告别可只有一条，并非每轮强行凑数。

实现使用 AstrBot 自带的分段回复，无新增第三方插件。分段用于私聊和群聊配置的模型回复，群聊通常写成 1–2 段；本群最近 60 秒已成功发送至少 6 条消息时，Rolebot 将后续普通回复完整合并为一条，数量下降后恢复分段；命令回复沿用原样。含代码围栏或超过 600 字符的内容整体发送，减少拆散代码和长文的问题。聊天历史仍保存一次完整回复，后续会话能连贯理解。

设置由 `scripts/lemuen.py` 维护，构建时写入 `runtime/lemuen/build/private-chat-settings.json`。需要改间隔时，也可在该私聊配置的“分段回复”页面调整。人格和表达参考规定短段落；框架负责实际发送，插件不另建消息队列。

## 本次验证

2026-09-24，在 njuse 的临时 AstrBot 4.27.4 实例使用 deepseek-v4-pro 复现仙人掌挞反馈，完成 3 轮实际回复：

- 用户原句在前五条中首先召回 L039；全部 7 道知识检索检查命中。
- 第一轮表达对仙人掌挞的嫌弃；问口感时没有编写味道；继续提出给她带一份时，回复“你这份心意我收下，仙人掌挞就还是免了。”
- 模型仍偶尔把未交代的“是否尝过”说成确定的“没尝过”，不能把本次喜恶修复当成所有事实边界均已理想化。
- 三轮均通过原生发送流程模拟为 2 条消息，合并后正文与模型输出一致。原有隔离测试通过。
- 临时实例没有 QQ 适配器，没有读取真实聊天或向 QQ 发测试消息，验证期间线上配置未改动。

原始回复与模拟消息只保留在 `runtime/lemuen/latest-smoke.json` 和 `.md`。知识库现有 69 个原作块；新增 L039 归入既有“生活兴趣”文档，未另建零碎知识库或常驻的仙人掌挞提示。

本次通过 SSH 部署，自动 CD 仍需能访问 njuse 内网的自托管 runner。
